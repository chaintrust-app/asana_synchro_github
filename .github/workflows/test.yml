on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello world action step
        uses: ./ # Uses an action in the root directory
        id: hello
        with:
          asana_secret: ${{ secrets.ASANA_ACCESS_TOKEN }}
          pr_name: ${{ github.event.commits[0].message}}
      - name: Get the name of PR
        id: name
        run: echo "The name of the PR is ${{ github.event.commits[0].message}} "
      - name: Get the state of PR
        id: state
        run: echo "The state of the PR is ${{ github.event.pull_request.title }} | ${{github.event.pull_request.merged}} "
